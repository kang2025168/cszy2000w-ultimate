# 项目工程审查 — 2026-09-22

## 范围与结论

检查了项目结构、Compose/启动脚本、交易执行核心、账户路由、资金管理、网格状态机、网页接口与现有测试。94 个 Python 文件完成 AST 语法检查；核心路径人工审阅，并非每一行均完成审计。现有 47 项 unittest 在禁用 socket 连接的进程中全部通过。没有启动服务、连接真实数据库或券商、提交订单或修改交易代码。

项目已具备多策略、资金池、风险控制、持仓同步、进程监管与看板。主要缺口是多个入口之间的交易一致性，以及故障场景的测试覆盖。应先修正确性和隔离边界，再做性能优化与模块拆分。本文不评价策略收益，也没有测量生产性能。

## 优先修复

### 1. P1：未成交买单可能被已有持仓误判为成交（已离线复现）

- `ultimate_v1/web_app.py:2560` 的 `_manual_order_fill` 在订单尚未成交时回退到当前总持仓。
- 模拟：已有 10 股、成本 100；新单买 5 股，订单为 `new`、成交量为 0。函数返回 `(5.0, 100.0, 'new')`。
- `web_app.py:2687` 的 `_record_manual_buy` 对不同订单号把返回数量加到已有数量，可能将本地 10 股错误更新为 15 股。
- `app/strategy_b.py:919` 的 `_reconcile_fill` 同样优先返回总持仓；盘后加仓在 1146 行使用该函数，有相同的成交确认问题。
- 修复：成交量以订单累计成交量为准，按已入账累计量计算增量；持仓只用于独立对账。订单重试与部分成交均需幂等入账。
- 验收：已有仓位但新单零成交不增加数量；部分成交、重复回报、延迟成交分别正确处理。

### 2. P1：专属账户缺少凭证时回退到通用账户（已离线复现）

- `ultimate_v1/account_config.py:123` 的 `_profile_with_env` 在 retirement 专属 key/secret 缺失时继续查找 PAPER/LIVE 与通用凭证。
- 用虚构的 trading 凭证、无 retirement 凭证模拟，retirement 确实得到 trading 凭证。key 与 secret 还分别回退，部分配置可能产生混配。
- 修复：专属账户凭证成对校验，缺失则阻断该账户；启动时读取并验证预期券商 account ID。不同 profile 指向同一账户时应明确识别，避免资金聚合重复计算。
- 本次未读取真实账户身份，不能断言目前已发生串户。

### 3. P1：未配置网页密码时允许访问交易接口（已离线复现）

- `ultimate_v1/web_app.py:8399` 的 `_authenticated` 在无认证 token 时返回 True。
- `/api/manual_stock_order` 等写接口依赖此认证，不像清仓接口另行校验操作密码。
- Compose 映射网页端口到所有宿主接口。实际互联网可达性取决于宿主机防火墙、网络及代理，本次未验证。
- 修复：无认证配置时拒绝启动交易控制服务，或强制只读；启用有服务端有效期的会话、登录限速和适合部署方式的 HTTPS/Secure cookie。当前 cookie 是固定 HMAC，浏览器 Max-Age 不构成服务端过期检查。
- MySQL 的 `3307:3306` 同样绑定所有宿主接口；若只供本机调试应绑定 localhost，容器内部访问不需要发布该端口。

### 4. P1：D 网格异常状态会被事务回滚（静态确认）

- `ultimate_v1/d_grid.py:555` 在 `db_conn` 事务内请求券商并更新状态。
- 异常分支写入 `ERROR` 与错误事件后重新抛出；`ultimate_v1/db.py:31` 随即 rollback，因此这些错误记录无法持久化。
- 若券商已接受订单而响应丢失、或随后数据库写入失败，数据库事务不能撤回外部订单。
- 已有稳定 client_order_id 是优点，但仍需按该 ID 查询恢复，不能只依赖重复提交报错。
- 修复：先持久化订单意图；外部请求与本地事务解耦；异常单独事务记录；重启后恢复处理中订单。避免持有数据库行锁等待外部网络。
- 验收：模拟提交成功但响应超时、提交后 DB 失败、进程重启，不重复下单且能恢复跟踪。

### 5. P1/P2：手动交易缺少统一资金预占与请求幂等（静态确认，尚未并发复现）

- `web_app.py:2890` 读取额度后直接提交订单；client_order_id 使用当前毫秒时间，每次重试得到新 ID。
- `capital_manager.py:530` 股票资金占用主要来自持仓；手动路径未看到针对未成交订单的原子预占。同一时刻的请求可读取相同的可用额度，券商账户 buying power 无法替代策略资金池配额。
- 手动路径没有调用自动策略使用的统一 `can_open_position`。需要明确哪些风控允许人工覆盖，避免不同入口隐含不同规则。
- 修复：按账户、资金池记录预占额度，事务内完成检查与预占；客户端请求携带持久化幂等键；订单终态释放或转化预占。卖出还需检查策略归属数量，不能只使用共享券商账户的整只股票总持仓。

## 性能与维护优化

### 优先做能直接减少重复工作的改动

1. `web_app.py:8111` 同时刷新多个接口；`_allocation_payload` 调用 `get_capital_allocation`，后者请求账户、刷新资金池并写数据库。多个浏览器会放大工作量。让后台集中刷新带时间戳的展示快照，网页读取快照；执行下单时仍做独立的新鲜度与额度检查。
2. `ultimate_v1/db.py` 每次读查询新建连接；`account_config.load_account_config` 又读数据库，gateway 每次创建客户端。测量后引入有界连接池和按账户隔离的客户端复用，明确线程安全和配置变更失效机制；增加 DB read/write timeout。
3. `allocation.used.get(group, get_strategy_used_capital(group))` 的默认表达式总会执行，即便 group 已存在；见 `app/bots/dashboard_bot.py:52` 与 `capital_manager.py:614` 附近。改为显式缺失判断可消除多余查询。
4. D 网格每次 `run_symbol`、看板每次刷新调用 `ensure_schema`；其中逐字段查询 information_schema 并运行建表检查。改为独立的版本化迁移，启动时验证版本，交易热路径不执行迁移。
5. 加上 API p50/p95、DB 查询次数、券商请求次数、下单确认耗时和最后成功心跳。上述性能建议基于代码结构，尚无线上耗时数据，不能承诺具体加速比例。

### 拆分重点

- `ultimate_v1/web_app.py` 8,678 行，混合 HTML/CSS/JS、鉴权、交易执行、SQL 和 API。先拆静态资源与只读接口，再迁移交易执行服务，保持行为一致。
- `app/strategy_b.py` 4,283 行。逐步分出配置、信号评分、下单、成交对账和退出规则。保留策略入口，减少大范围改写风险。
- 统一交易执行服务：账户路由、统一风控、订单意图、资金预占、券商提交、成交入账、故障恢复。已有 C 策略执行锁、D 网格状态机和风控模块可以复用设计。

### 配置、依赖与运维

- README 的 B_MAX_ENTRY_UP_PCT 是 0.04，代码默认值为 0.4；B_REQUIRE_INTRADAY_VOLUME 文档为 1，代码为 0。需要明确默认值、覆盖顺序并输出脱敏有效配置，不能据此推断实际部署值。
- README/PROJECT_MAP 的默认启动说明与 profiles 不一致；tradebot 的 legacy 入口调用 main，但 run.sh 的 main 已明确退出。重新部署应按真实拓扑编写命令并验证。
- requirements.txt 重复声明 pandas-datareader；requests-cache 无版本约束。建立可复现依赖清单与独立开发依赖。没有做依赖漏洞扫描，不作漏洞结论。
- `.gitignore` 缺少 `.venv/`、`.pytest_cache/`、`.DS_Store` 等；仓库仍跟踪若干备份文件。归档旧入口、减少版本混淆。
- Compose 已有日志轮转与重启策略，这是优点。生产还应验证数据库备份恢复、磁盘容量告警、心跳过期告警和订单对账异常告警。

## 验证记录与实施顺序

本次：94 个 Python 文件语法通过；47/47 单元测试通过；三项独立离线复现。测试只验证已有测试场景，不代表实盘安全或盈利。

第一批：修正成交确认、严格账户隔离、认证默认拒绝，并补精确回归测试。

第二批：订单意图、请求幂等、资金预占、异常恢复与 D 网格事务边界；用模拟券商和临时 MySQL 测试部分成交、超时和重启。

第三批：展示快照、消除重复查询、移出热路径迁移，再测量性能；同步修复文档与配置漂移。

第四批：分模块整理网页与 B 策略，建立 CI；在 paper 环境验证订单生命周期后再考虑部署。
